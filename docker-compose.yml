version: '3.8'

services:
  app:
    build:
      context: .
      args:
        GEMINI_API_KEY: ${GEMINI_API_KEY}
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    restart: always
